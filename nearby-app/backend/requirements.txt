# requirements.txt

# Core FastAPI and server
fastapi
uvicorn[standard]

# Database
sqlalchemy
psycopg2-binary
alembic
geoalchemy2[shapely]
pgvector

# Configuration and Security
pydantic[email]
pydantic-settings
python-dotenv
passlib[bcrypt]
python-jose[cryptography]

# Rate limiting
slowapi

# Utilities
python-multipart
bleach
python-dateutil

# Image processing
Pillow
aiofiles

# Cloud storage (for S3 integration)
boto3
botocore

# Error tracking
sentry-sdk[fastapi]

# Testing dependencies
pytest
httpx

# Machine Learning / Embeddings
# Embeddings are served out-of-process by a TEI container and consumed via the
# fail-soft HTTP client in shared.embeddings — no in-process model is loaded,
# so torch / sentence-transformers are no longer bundled (large image-size win).
# `transformers` is kept only as a lightweight tokenizer-side dependency.
transformers